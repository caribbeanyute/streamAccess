package me.cleon.streamaccess.config;

import java.time.ZoneId;

public class Constants {
    public static final ZoneId ZONE_ID = (ZoneId.of("UTC"));
    public static final long TOKEN_EXPIRATION =  30 * 60 * 1000;
}
